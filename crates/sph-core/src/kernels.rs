// Poly6, spiky-gradient, and viscosity-laplacian kernels.

use crate::math::Vec3;

// Kernels — pure scalar/vector math, operate on individual r/r_vec values regardless of layout
pub fn poly6(r: f32, h: f32) -> f32 {
    todo!()
}

pub fn spiky_gradient(r_vec: Vec3, h: f32) -> Vec3 {
    todo!()
}

pub fn viscosity_laplacian(r: f32, h: f32) -> f32 {
    todo!()
}
